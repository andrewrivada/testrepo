



1. Create a project folder (if it does not exist)
2. Initialize folder as a repository (While inside the folder in cmd type: git init )
3. "Put it onto the stage" git add . // . signigfies all or you can use -a
4. Then commit it to the repository : git commit -m "This contains the message for the patch" // 
5. 